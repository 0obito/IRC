
// Created the server class backbone, Youness will have to fill this and complete it!

#include <string>

class Server {
    Server();
    Server(const Server& other);
    const Server& operator=(const Server& other);
    ~Server();
};
